# Dev-team memory log

## 2026-09-12 — dev-team-auto — Card schema for every .LIN card
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-analyze sonnet/high, dt-engineer opus/high — lane-card-schema
- **What happened:** CardSchema now has names/kinds/appearance rule for all 48 deck labels plus solver/.mdb-only cards; Deck.Validate() returns (line, label, reason). 56 -> 90 tests. Orchestrator mutation-checked fixed-count check, variable-rule check, and a removed entry; all went red.
- **What worked:** ATB 3I column titles live in `~/atb-work/p0/msi/General Dynamics/ATB3I/ATB3iData.mdb` (mdbtools reads it); the solver's read order, not .mdb column order, is token order (e.g. F.1.B Edge Test before Output).
- **What failed:** dt-analyze reported no .mdb exists — wrong; the engineer found it. Seed D.5 was missing EllipID (13 tokens, not 12); seed D.2 key matched no deck label (decks use D.2.A-D).
- **Remember next run:** build baseline is 27 nullable warnings in Atb.App/Solver (SolverRun.cs, Win32.cs), not 0. Only ONE H.1.a-mislabelled empty H.3 row exists (2479_2.LIN:321), not three — the labeler item's done-when says three. Validate() is per-line only; cross-card totals (D.7 vs segment count) unchecked. B.1 field 4 is Flexible Bodies (solver), not Symmetry.

## 2026-09-12 14:50 — dev-team-auto — Deck labeler for unlabelled or mislabelled decks
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — lane-card-schema, 0a1763c
- **What happened:** New `Atb.Core/Lin/Labeler.cs` walks ATB 3I FileManager.ReadFile order/counts, labels blanks and non-schema labels, fixes the empty-H.2/H.3-as-H.1.a bug. 90 -> 114 tests. Orchestrator mutation-checked skip-labelled guard, F.3.B count, last-card error text, H-bug condition; all four went red on their own test.
- **What worked:** asserting the grammar agrees with every existing client-deck label (proves no desync); swapping only the label text inside Raw keeps relabelled lines byte-identical elsewhere.
- **What failed:** none. Done-when says three H.1.a-mislabelled rows; only one exists (2479_2.LIN:321). H.1 continuation rows labelled H.1.a are ATB 3I's normal layout, not the bug.
- **Remember next run:** F.8.B is written unlabelled by ATB 3I (treated as a data row). SetLabel drops tokens past the card's count (ejection.lin H.10.c 12 -> 10 values) — ATB's reader ignores them. D.3/D.4 belts/airbags and F.9 water throw as unsupported; B.3.C, D.6 Type 5 second line, H.12 multi-HIC layouts are unverified guesses (ponytail comments).

## 2026-09-12 — dev-team-auto — Generic card grid screen
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — lane-card-schema, 3ec88ea
- **What happened:** Deck.Edit (Deck.cs:109, rewrites only that line), Deck.ParsePaste/ToPasteText (:155/:192), CardSchema.TryHeader/Screens (CardSpec.Tail repeats variable groups), MainForm grid with left card list from scope-table titles. 114 -> 141 tests. Orchestrator mutation checks all went RED on the intended assertion: (a) removing `why ??= spec.Check(toks)` -> ParsePaste_LabelsRows Assert.Single on Lines; (b) also Set()ing the neighbour line -> Edit_Segment1Weight Raw==null index list [7] vs [7,8]; (b2) Save appending a space to Format -> same test's whole-file compare; (c) TryHeader null for B.2.B col 0 -> EveryDeckColumn_HasSchemaHeader only (pinned-total stayed green). (d) Wiring read: `CopyRows` -> `Clipboard.SetText(Deck.ToPasteText(rows, screen.Cards))`; `PasteRows` -> `Deck.ParsePaste(Clipboard.GetText(), screen.Cards)` then `InsertLines(res.Lines, "Paste")`; `OnCellChanged` -> `deck.Edit(line, t, v)`, on error resets cell to `line.Str(t)`; `AddRow` -> `InsertLines(... new DeckLine(l.Tokens, l.Label) ...)`; `DeleteRows` -> `deck.Lines.Remove(l)`.
- **What worked:** whole-file byte compare plus "only the edited line has Raw==null" gives two independent guards; copy->paste round-trip over every screen on all 12 decks.
- **What failed:** none.
- **Remember next run:** add/delete/paste do NOT update count cards (B.1, D.1.a, H.10.a MCG) — solver may reject; renumbering item owns it. C.3, E.4/E.6/E.7 tables, F.8.B unlabelled rows and B.1 are on no grid yet. RefNames shares one function-ID namespace across E.1/E.6/E.7 (ponytail). SchemaHeaderTotal_IsPinned=470 must be bumped when a real field is added. H.1.b lone-0 placeholder rows are dropped by copy->paste. Incremental `dotnet build` shows 0 warnings; the 27 baseline warnings only appear on a clean build.

## 2026-09-12 — dev-team-auto — Run action
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/high) — lane-card-schema, commit 2d2d355
- **What happened:** New Atb.Core/Solver/SolverJob.cs (Answers(mode,in,out), Finish(work,base,deck,mode,cancelled), Outputs/IsOutputExt moved from SolverRun). MainForm File > Run ATB / Convert .ain to .lin go through RunSolver → SolverRun (FeedMode.Stdin) in a Guid subdir of ShortWorkRoot(); RunProgressForm with Cancel. Tests 141 → 147; Solver warnings 23 (clean rebuild), not up.
- **What worked:** Orchestrator mutation checks on SolverJob.cs, cp-backed plus diff -q: (a) drop DeleteDir → 3 Finish tests red; (b) return early on cancel without cleanup → only Finish_Cancelled_LeavesNothingBehind red; (c1) "l"→"L" → only Answers_RunLin_PinnedToSolverRunAt691575f red; (c2) 102 "y"→"n" → only Answers_ConvertAin red. Restored byte-identical. Call sites checked: SolverRun.cs `string[] answers = o.Answers ?? SolverJob.Answers(o.Job, o.InputBase, o.OutputBase);` (inline array deleted); MainForm `(r, outs) = await Task.Run(() => { ... sr.Run(o); ... SolverJob.Finish(work, b, input, job, cancelled: res.Error == "cancelled") })`; `prog.CancelRequested += () => Task.Run(sr.Cancel);` (Kill(true) process tree); `file.DropDownItems.Add("Con&vert .ain to .lin...", null, async (_, _) => await ConvertAin());` → RunSolver(SolverMode.ConvertAin, ...) → Open(<base>.lin). Status lines go to the UI via BeginInvoke in RunProgressForm.Post.
- **What failed:** none. Baseline warning count in spawn prompt was 27; the actual unique count at baseline was 24.
- **Remember next run:** The mode-102 stdin sequence ["y","","a","y",in,out] comes from src/input_files.for prompts (:66,:118,:155,:176,:232,:262). It was never run against the real solver, so the Windows human check is the only proof. The Run menu uses Stdin, never Handoff: Handoff writes C:\ATBFIG.SYS and sweeps System32. Human check on cases/2479 must copy the deck out of cases/ first (Finish overwrites next to the deck). Convert overwrites an existing <base>.lin without asking. If the Task.Run body throws before Finish (e.g. the deck copy fails), the scratch dir is left under ATBRun. The progress window shows SolverRun log lines, not raw solver stdout (the QuickWin solver draws its own window). Row add/delete count cards are still owned by the renumbering item.

## 2026-09-12 15:20 — dev-team-auto — Animation viewer
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — lane-card-schema, commit 07b8276
- **What happened:** Added `Atb.Core.Sa1.Playback.FrameAt` (pure binary search) and `Sa1File.BeltStrands(i)` (the single belt world-point function). Rewired Sa1Scene/AnimationForm to use them; ColorMap defaults, step/speed controls, per-object colour. Fixed the MakeHyperElip port: all powers are set to 2 when every power is <= 2 (UISub.cs:55). Tests 147 -> 165.
- **What worked:** Sled bbox written out as literals from sledout.sa1's plane corners (x -7.77..60, y +-12, z -48.97..-10). Tightened z max from -0.75 (full plane box) to -10 (seat pan top); the loose box let an untransformed strand pass. Self-verified mutations, each RED on the intended assertion and restored byte-identical via cp: (a) loader duplicates frame 1's time -> "frame 1 time 0 not after 0"; (b) strand 1 skips its transform -> "strand 1 point ... z outside sled box"; (c) `hi = times.Count - 2` -> Playback_LastTime_IsLastFrameAndClamps (+ Case2479_FirstMidLast). (d) quotes: Sa1Scene `cam.Transform = camFrame;` + `camFrame.Matrix = AxisInv * entry[CameraSegment].Matrix * Axis;`; `var strands = sa1.BeltStrands(i);`; AnimationForm `Playback.FrameAt(times, wall, (double)speed.Value)`; grep for Split/Parse(/ReadAll/StreamReader/File.Read in Viewer/ finds nothing.
- **What failed:** Nothing. The prompt's "24 baseline warnings" read as 26 on a --no-incremental build; all are in untouched Solver/SolverRun.cs + Win32.cs.
- **Remember next run:** Human decisions open: (1) the axis map keeps ATB 3I's (x,y,z)->(x,-z,y), so solver +Z (down) points down on screen; the guardrail "Z up on screen" is read as "Z axis vertical". (2) The segment camera looks at the segment ellipsoid centre, because ATB 3I's saved-settings defaults are (0,0,0); entering exact camera coordinates is not implemented. WPF human checks are still pending on Windows.

## 2026-09-12 15:59 — dev-team-auto — ID renumbering
- **Outcome:** DONE — 2 attempts — caution: yes — team: dt-engineer opus/high→xhigh, dt-qa opus/high→xhigh, dt-review opus/high→xhigh — lane-card-schema, 66ec1c8
- **What happened:** Renumber.cs inserts/deletes segment/joint/plane/vehicle, shifts g/j/p/e-marked refs (new ellipsoid-ref kind; D.2.A plane, H.7/H.9 joint marks), updates B.1/D.1.A/D.1.B/H counts, confirm callback lists ref sites; MainForm wired. QA PASS 181; review 1/1/3 (negative H refs, H.1-H.3 zeroed on delete) → fix pass → delta QA PASS 189, delta review 0/0/2.
- **What worked:** Python oracle for the 130 changed lines of the insert; QA cross-checking ABS usage per field against src/*.for.
- **What failed:** first pass compared H refs signed though the solver reads SEG(ABS(KK)) on H.1-H.8 Segment/Joint fields; H.1-H.3 rows were zeroed (SEG(0)/vehicle frame) instead of dropped.
- **Remember next run:** ABS applies to H.1-H.8 Segment, Segment-or-Joint and H.7 Joint only — Ref Segment, H.9 joint, H.10.B are signed. H.n.B lines are found positionally after H.n.A (2479_2.LIN:321 H.3.a is mislabelled H.1.a). Still open: paste on entity screens doesn't renumber; F.10 delete doesn't shift H.11 actuator indices; D.4/F.2.B/F.6/F.9* unmarked in schema (ATBUpdate.cs:223-253); D.6 Type 5 follow-on drop unverified. H cards are renumbered though ATB 3I doesn't. STANDARDS.md added at repo root by the reviewer.

## 2026-09-12 16:45 — dev-team-auto — GEBOD body generator
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — lane-card-schema, f49f3a0
- **What happened:** Gebod.Answers(GebodRequest) in Atb.Core (app/Atb.Core/Solver/Gebod.cs) builds the stdin lines, using the prompt order in the exe's code. GebodForm copies ATB 3I's GEBOD V.2 form and its 32-dimension dialog. The exe and GEBOD.DAT are in frontend/bin; their shasums match the 1300 patch (618439cf…, 99a753a1…). The csproj has a None item with CopyToPublishDirectory and ExcludeFromSingleFile. A `!frontend/bin/` negation was added to .gitignore. Tests: 200, up from 189. Build: 0 errors.
- **What worked:** Call sites. MainForm.cs:48 `tools.DropDownItems.Add("&GEBOD...", null, async (_, _) => await Gebod());`. MainForm.cs:364 `new RunOptions { ExePath = exe, WorkDir = work, ... Mode = FeedMode.Stdin, ... Answers = Atb.Core.Solver.Gebod.Answers(req), ResultFile = "GEBOD.ain" ... }`. MainForm.cs:403 `await RunSolver(SolverMode.ConvertAin, dest);`. The win-x64 single-file publish runs on macOS and puts ATB.exe, Gebodv.exe and GEBOD.DAT side by side in the output folder. Mutations: the engineer ran (a)-(c) and each went red only on the intended test. The orchestrator re-ran its own check: changing the percentile literal to 95.0 turned exactly 1 test red.
- **What failed:** none
- **Remember next run:** GEBOD.ain holds only the B (body) cards. ATB 3I merged them into a full deck (atbusrguide.txt:273-276), so converting GEBOD.ain on its own probably fails or gives an incomplete .lin. This is marked with a ponytail comment at MainForm.cs:402 and needs a human decision. Gebodv.exe needs C:\ATBFIG.SYS: the app writes the file and restores the old one afterwards, and writing to C:\ may need admin rights. The exe only accepts percentiles strictly between 1 and 100 (ATB 3I's form allows 0-100). The exe asks for a description, which ATB 3I's form lacks, so the subject type name is sent. The exe asks the joint question for adults even when the form hides it. Numbers are always sent with a decimal point. atb-win32.exe does NOT reach the publish folder. SolverRun gained a ResultFile option. The lower-arm choice shows up slightly earlier than in ATB 3I.

## 2026-09-12 — dev-team-auto — Run/Convert Save-as dialog + SolverJob.Finish success gate
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — r1b-run-overwrite, commit 7e1ea59
- **What happened:** RunSolver shows a "Save results as" SaveFileDialog (OverwritePrompt, *.aou/*.lin) before any work; cancel aborts. Finish is now (workDir, outBase, destDir, destBase, mode, succeeded) and renames outputs to destBase on copy. A throw inside Task.Run calls Finish(succeeded:false), so the scratch dir is gone on every path. 200→203 tests.
- **What worked:** The solver still runs on the input's own base (Convert names the .lin after its input), so Finish does the rename. Mutations: gate flip → 6 red, incl. Finish_FailedRun_KeepsPreviousOutputsByteIdentical; ignoring destDir → 3 red, incl. Finish_CopiesToChosenDirAndBase_NotDeckDir.
- **What failed:** none
- **Remember next run:** The GEBOD follow-up Convert (MainForm ~403) goes through RunSolver, so it now shows the Save dialog too. RunDeck still auto-saves a dirty deck to its own path before Run (the deck itself, not solver output).

## 2026-09-12 17:47 — dev-team-auto — Round 1b renumbering gaps
- **Outcome:** DONE — 4 attempts — caution: yes — team: dt-engineer opus/high then xhigh x3, dt-qa opus/high x4, dt-review opus/high x4 — r1b-run-overwrite, commit 7a52820
- **What happened:** Paste routes through Renumber.Paste→Insert per row; H.11 actuator kind (abs compare, drop, Count, H.11 line removed at NRTORQ 0); F.2.B/F.6/F.9 marks; D.6 Type 5 one line (Renumber + Labeler). Review fixes: Validate flags H.11 Count 0 with NRTORQ>0 (STOP 741) and zeroed F.9 refs; MainForm Run now blocks on Validate, Save asks "Save anyway?"; paste rejects joint rows whose spin block (B.3.A type, B.4/B.5 lines) doesn't match the template, all-or-none.
- **What worked:** delta QA + delta review after every fix pass; QA writing failing tests for the review's finding before the fix.
- **What failed:** spin-class paste check took 3 passes (B.3.A only → B.4/B.5 presence only → half-filled B.5 block). Should have asked for all-or-none on every joint screen in one pass.
- **Remember next run:** D.4 carries no segment ref (ATBUpdate.cs:227 updates AirbagID, never written to .LIN; input_airbags.for:98-105) — the done-when's "D.4 refs shift" is tested as "D.4 untouched"; human decision. Save/Run never called Deck.Validate before this item. Open: Run has no override (a solver-acceptable deck failing a per-card check can't run); half-filled B.5 row gets the wrong-Joint-Type message; hand-built rows with repeated cards bypass the spin check; Labeler still throws on NBLT/NBAG/NWATER>0.

## 2026-09-12 17:52 — dev-team-auto — Round 1b validate before Save and Run
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — r1b-run-overwrite, 77b20ba
- **What happened:** Moved the issue text into `DeckIssue.Format` (Atb.Core, 20-row cap + "... and N more"); Save and Run both warn with OKCancel (Cancel default); Save body split into `Write()` so Continue writes exactly what Save wrote and Run doesn't ask twice. Convert not wired (it takes an .ain, not the open deck).
- **What worked:** Literal-string asserts for 0/1/20/21 issues; orchestrator mutation (drop line number) reddened exactly the 3 new formatter tests and nothing else.
- **What failed:** none
- **Remember next run:** The `done when:` naming Convert was N/A — plan criteria that name UI paths without an open deck should be checked against the code before the lane is written. Dialog not exercised on Windows; build-only verification for MainForm.

## 2026-09-12 19:10 — dev-team-auto — Round 1b Windows UI test robot
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer (opus/high) — r1b-run-overwrite, 5d255d5 (code da65bef)
- **What happened:** Built app/Atb.App.UiTests (FlaUI, out of Atb.sln) and app-e2e.yml (calls windows-build.yml via workflow_call). Green run https://github.com/bcn-services/bcns-client-technology-associates-atb/actions/runs/34731451037, 4 of 8 pushes: 12 edit+Save decks pass the single-token diff, CROSS PASS on 2479_2 (app vs direct, 9 files), 13 .sa1 files viewed with no dialog, 140 screenshots under shots/<scenario>/. The engineer stopped twice while a CI run was pending and had to be resumed.
- **What worked:** tokendiff.py and cross_gate.py as small gate scripts, mutation-checked locally. The truncated-.sa1 control scenario proves the dialog detector trips. The Save dialog path is typed by keyboard.
- **What failed:** A segment-only insert passes Deck.Validate but the solver stops at STOP 24 (it wants one joint per extra segment, so it is one G.2 card short). The robot inserts segment and joint together. Human decision: should a segment insert add a joint, or should Validate reject the deck?
- **Remember next run:** Gebodv.exe fails without C:\ATBFIG.SYS (exit -1073741795, "file not found, unit 7"). With the file it exits 0 and writes GEBOD.ain, now at app/Atb.Core.Tests/fixtures/gebod-50m.ain. The app-e2e artifact is `app-e2e`. When briefing a background engineer, tell it to block on `gh run watch` in the foreground and never return while a run is pending.

## 2026-09-12 — dev-team-auto — Round 1b D: GEBOD merge into open deck
- **Outcome:** DONE — 2 attempts — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high (attempt 2 = same agents resumed, effort stayed high, not escalated to xhigh) — r1b-run-overwrite
- **What happened:** GebodMerge.Merge(Deck, ain, placement, confirm) + File > New + GebodForm placement + MainForm merge. Attempt 1 QA PASS, review 0/2/4 (Replace silently dropped refs; GEBOD.ain lost on merge failure). Fix pass → review 0/0/2, QA PASS 261/261; local macOS src/atb accepts all 8 merged decks (STOP 1).
- **What worked:** running dt-review concurrently with dt-qa (no edits). QA running merged decks through the local macOS solver `~/bcns-client-technology-associates-atb/src/atb` — real solver acceptance on mac, no Windows needed. ATB_QA_DUMP env in GebodMergeQaTests writes the decks.
- **What failed:** criterion wording "grows B.1 by GEBOD joint count" is wrong for a deck with a body: NJNT grows by 15 not 14 (NULL joint at s0-1, as 3I does). Replace-body wording in the done-when implied dropping refs.
- **Remember next run:** Local mac solver exists at repo src/atb — use it as the real bar. Body starts = [1] + J+1 per NULL joint (Seg JNT 0, src/Chain.for:38-43); one G.2 per body. Solver divides by G (Inrtia.for:122) → New deck G=386.088. Gebodv.exe still needs C:\ATBFIG.SYS (kept). Open human decisions: Replace removes refs to the replaced body vs 3I's remap to the new body; File > New has NSTEPS=0 (3I default) so New→GEBOD→Run produces no .sa1; grid Insert-segment without joint (STOP 24). Minors left: Replace confirm omits H11Emptied (STOP 741) warning; MainForm re-check compares body count, not BodyStarts. UI not exercised on Windows.

## 2026-09-12 20:30 — dev-team-auto — Round 1b E full Windows e2e + GEBOD scenario
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — r1b-run-overwrite, code bcfad2b + checkpoint
- **What happened:** NewGebodSaveRun UI scenario (New > GEBOD 50th male > Add > Save As > Run, asserts SolverJob.AouEndedNormally). Viewer scenarios gained a torso-follow camera shot (framing choice b: our View all matches 3I, FileManager.cs:2718 / Animation.cs:1195-1197). Green run https://github.com/bcn-services/bcns-client-technology-associates-atb/actions/runs/34735611144, 29/29 UI scenarios, 165 screenshots listed in engineer-report.md. 3 of 5 pushes.
- **What worked:** AccessibleName on combos + Choose scrolling/keyboard fallback; mutation on AouEndedNormally reddened its unit test.
- **What failed:** push 1 failed (Camera combo item off-screen); push 2 green but follow on a limb lost the body. Sonnet screenshot readers shifted descriptions by 1-2 files — flagged frames needed opus re-reads. Harness blocked the subagent writing its report file; orchestrator wrote it.
- **Remember next run:** Open human decisions: New+GEBOD has NSTEPS=0 so no .sa1/viewer (objective conflict); 3I-style rigid torso camera gets occluded by planes in view-2479_2/05 and view-2638_135_Restart_2a/06-07. Follow shots land on frame 1 after step-forward (cosmetic). Give screenshot readers small batches and have them echo the filename they read.

## 2026-09-14 — dev-team-auto — GEBOD Replace follows ATB 3I
- **Outcome:** DONE — 2 attempts — caution: yes — team: dt-analyze sonnet/high, dt-engineer opus/high then opus/xhigh, dt-qa opus/high then opus/xhigh (delta), dt-review opus/high then opus/xhigh (delta) — r2-gebod-replace (worktree .claude/worktrees/r2-gebod-replace)
- **What happened:** Replace now maps refs by position; surplus via Renumber.Delete, extras via Renumber.Insert; body cards swapped in place; only 3I's Body.cs:1049 confirm. Attempt 1 QA FAIL: engineer added a second post-run "dropped refs" dialog 3I lacks; review 0/3/2. Fix pass removed it, fixed the STANDARDS divergence line, made ReplacedReferences list F.10/H.11 + STOP 741, Swap throws on a missing G.3.A/B.4.A/B.5.A. Delta QA PASS 1176, delta review 0/0/2. Green e2e https://github.com/bcn-services/bcns-client-technology-associates-atb/actions/runs/34908386359.
- **What worked:** QA planting H.7/H.9 joint refs (2479 has none outside bodies); QA replacing every body of all 12 cases/ decks as a no-throw sweep; local src/atb STOP 1 on 9 merged decks.
- **What failed:** engineer invented a UX safety dialog against "copy 3I exactly" — brief engineers that 3I's silence is the spec. First STANDARDS divergence note named the wrong body.
- **Remember next run:** Real 3I divergence: body 1 of a multi-body deck — 3I's DeleteBody joint range includes the next body's NULL joint (GEBOD.cs:1797, Body.cs:1097-1103); we keep joint i→i (STANDARDS.md:23). InsertSegmentRun is flaky: RunDeck (Robot.cs:239-241) stops waiting before the late "Open the animation?" dialog (run 34907818557). Open Minors: Swap only catches a missing group at the list end, not mid-list (count G.3.A==NSEG, B.4.A/B.5.A==NJNT once in Merge); no client deck has a body longer than a GEBOD body, so shorter-body tests build the .ain from gebod-50m.ain cut to n segments.

## 2026-09-14 — dev-team-auto — Segment and joint insert/delete warning, as ATB 3I
- **Outcome:** BLOCKED — 1 attempt — caution: no — team: dt-engineer opus/high — r2-gebod-replace, code f18eea3 + checkpoint da0d0e4 (pushed)
- **What happened:** 3I cascade texts/titles as `Renumber` constants (verified vs TableForm.cs:412-440), pure seam `Renumber.CascadeConfirmed(e, detail, ask)`, MainForm AddRow/DeleteRows/PasteRows ask once for segment/joint only; delete = one dialog, 3I text + reference list (new `Renumber.DeleteRefs`). 1180 passed. Robot gate never ran: Actions run 34910052325 refused to start ("recent account payments have failed or your spending limit needs to be increased").
- **What worked:** pure ask seam made the No-path unit-testable; mutations (a) `ask(...) | true` reddened only No_IsNotConfirmed, (b) one space removed reddened only Texts_AreAtb3IStrings.
- **What failed:** GitHub Actions billing on bcn-services org; `|| true` first mutation also skipped the ask and reddened two tests (redone isolated).
- **Remember next run:** After billing is fixed re-dispatch `GITHUB_TOKEN= gh workflow run app-e2e.yml --ref r2-gebod-replace -f filter='FullyQualifiedName~InsertSegmentWarning|FullyQualifiedName~InsertSegmentRun|FullyQualifiedName~GebodReplaceBody1Run'` and read the warning screenshots; new InsertSegmentWarning scenario and edited InsertSegmentRun are unproven on Windows. Human call: combined delete dialog drops ConfirmDelete's trailing "Delete it anyway? ..." sentence (3I's "Continue?" asks). engineer-report.md was tracked in git despite .gitignore — untracked now.

## 2026-09-14 18:10 — dev-team-auto — Body Summary screen (§2 #6)
- **Outcome:** DONE — 2 attempts — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high, dt-engineer opus/xhigh, dt-qa opus/xhigh, dt-review opus/xhigh — r2-gebod-replace, e6b965d
- **What happened:** Built BodyForm plus Atb.Core/Cards/Bodies.cs. Attempt 1 passed every criterion but QA failed the guardrail, because Bodies.cs did its own reference arithmetic. The review also found 2 Important issues: the clipboard survived a deck replace, and card families were copied by position with no count guard. Attempt 2 moved placement into Renumber.CopyBody/Place with placeholder tokens. After that QA PASSED, the review was 0/0/0, and Windows run 34914745232 was green.
- **What worked:** Running QA and review in parallel. The placeholder-token placement inside Renumber. A family row-count check that takes its families from Renumber.Groups().
- **What failed:** In attempt 1 Bodies.Insert/Replace computed shifted references outside Renumber, which violated the guardrail.
- **Remember next run:** Done-when 1 names 2638_Start_135_, but that deck has one body; the test used 2495_2 instead (a human decision). The buttons follow 3I's on-screen layout, not the order the done-when lists. The GEBOD buttons ask once, as 3I does (lane review: GEBOD.cs has no prompt); 3I's Replace-with-GEBOD deletes the body before GEBOD opens, ours doesn't (in STANDARDS.md). Renumber.Live is pinned by BodiesTests.CopiedBodyRefsOutsideTheBody_FollowTheInsert (52/16). Copied references point at the copy (in STANDARDS.md). Surplus B.3.A becomes -1. The robot never presses Yes on Insert/Replace copied. The UiTests keep a duplicate of 3I's texts. RunDeck now waits 10 s for the "ATB run" box.

## 2026-09-14 — dev-team-auto — Maximum Value List screen (§2 #38)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-engineer opus/high — r2-gebod-replace, 741b3f9
- **What happened:** MaxValues constant (21 Setting rows, ID order per MainMenu.cs:4304), read-only MaxValueForm under File > Setting (MainMenu.cs:2655-2659), robot scenario MaxValueList. 1194 passed; Windows run 34916340763 green. The orchestrator handed back before its engineer finished; the session lead verified: mutations on one value and one name each reddened only Rows_AreTheSettingTableInIdOrder, file restored byte-identical.
- **What worked:** Literal-rows test independent of the constant; robot types into a cell and asserts 80 stays (the editable-grid mutation guard).
- **What failed:** The orchestrator returned while its background engineer was still running (no foreground wait on the child).
- **Remember next run:** 3I's grid is editable (GridMode 4, ATBGrid.cs:614-627, Save → UpdateTable :1520); ours is read-only per TIER2-SCOPE #38 (in STANDARDS.md). Human call: read literally, 3I's hidden-column rules show Value | FileID | ZzzKey with no names; we show Value | Name. Tell orchestrators to block on their engineer, not return "waiting".

## 2026-09-14 19:50 — dev-team-auto — Vehicle Motion list and sub-editors (C.1–C.5)
- **Outcome:** DONE — 1 attempt — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high — r2-s2
- **What happened:** Built Atb.Core Cards/Vehicles.cs (VehicleType copy of Vehicle.cs:975-994, Cell/EditCell, PlotPoints as sole plot source), VehicleForms.cs (VehOpt1/VehOpt2/VehOpt34 + GDI Data Plot), robot theory VehicleMotionEditors (6/6 green, run 34920934490). QA PASS (1233 tests), review 0/0/4.
- **What worked:** census test with literal counts caught the rule mutation; QA's file-bytes one-line diff and literal Cell addresses tightened engineer tests that re-derived from Vehicles.Cell.
- **What failed:** none blocking. Minors open: Cell-derived expectations in VehiclesTests/Scenarios.cs:412 (QA added literal tests); NRE on negative-interp deck without C.2.B or short C.5a rows (VehicleForms.cs:290-310); undisposed Pens on Paint; dead SegId branch (VehicleForms.cs:120).
- **Remember next run:** Human calls: Insert/Copy/Delete/Replace Vehicle buttons and grid row add/delete are DISABLED (STANDARDS.md) — conflicts with "matches 3I"; VehOpt2/34 General-tab control positions approximated (real coords in decompiled InitializeComponent). Done-when "client deck": no deck has types 0/2/3 — half-sine, 6-DOF, spline-position use synthetic fixtures (app/Atb.Core.Tests/fixtures/vehicles/); C.3 does exist unlabelled in corpus/2210_1..4 (analyzer's "zero" was wrong). 3I's VehOpt1 has no grid/plot. Census: 4 type-1, 9 type-4, 147 type-5 vehicles across 139 decks.
